// Re-exported from the domain layer. Kept so existing imports of
// "@/lib/resolve" keep working until the old screens are removed in Phase 11.
export { rankSlots } from "@/lib/domain/rank";
export type { SlotScore } from "@/lib/domain/rank";
